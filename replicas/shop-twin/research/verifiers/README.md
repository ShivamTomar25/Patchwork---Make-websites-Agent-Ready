# ShopTwin Verifiers

Use `POST /api/research/verify/SHOP-J1`, `SHOP-J2` or `SHOP-J3` while authenticated as the seeded admin. Results are machine-readable and include predicates, violations, authoritative state and current defect configuration.
