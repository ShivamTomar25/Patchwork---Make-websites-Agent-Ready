# SupportTwin Verifiers

Use `POST /api/research/verify/SUPPORT-J1`, `SUPPORT-J2`, `SUPPORT-J3` or `SUPPORT-J4` while authenticated as the seeded admin. Results are machine-readable and include predicates, violations, authoritative state and current defect configuration.
