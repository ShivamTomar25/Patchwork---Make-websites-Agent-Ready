{
  "name": "@patchwork/support-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "DATABASE_URL=${SUPPORT_DATABASE_URL:-postgresql://${USER:-postgres}@localhost:5432/patchwork_support} PORT=4103 node --import tsx src/index.ts",
    "build": "DATABASE_URL=${SUPPORT_DATABASE_URL:-postgresql://${USER:-postgres}@localhost:5432/patchwork_support} tsc --noEmit",
    "prisma:generate": "DATABASE_URL=${SUPPORT_DATABASE_URL:-postgresql://${USER:-postgres}@localhost:5432/patchwork_support} prisma generate --schema prisma/schema.prisma",
    "db:migrate": "DATABASE_URL=${SUPPORT_DATABASE_URL:-postgresql://${USER:-postgres}@localhost:5432/patchwork_support} prisma migrate deploy --schema prisma/schema.prisma",
    "seed": "DATABASE_URL=${SUPPORT_DATABASE_URL:-postgresql://${USER:-postgres}@localhost:5432/patchwork_support} node --import tsx src/seed.ts",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@patchwork/api-kit": "0.1.0",
    "@patchwork/shared": "0.1.0"
  }
}
