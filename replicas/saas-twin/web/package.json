{
  "name": "@patchwork/saas-web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 3102 --strictPort",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host 0.0.0.0 --port 3102"
  },
  "dependencies": {
    "@patchwork/shared": "0.1.0",
    "@patchwork/web-kit": "0.1.0"
  }
}
