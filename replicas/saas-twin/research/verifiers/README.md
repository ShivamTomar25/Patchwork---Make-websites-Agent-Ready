# SaaSTwin Verifiers

Use `POST /api/research/verify/SAAS-J1`, `SAAS-J2`, `SAAS-J3` or `SAAS-J4` while authenticated as the seeded admin. Results are machine-readable and include predicates, violations, authoritative state and current defect configuration.
